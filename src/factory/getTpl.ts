/** 获取项目脚手架初始化模版
 * @param {ICliConf} cliConf 要格式化的日期数据，为空时默认为当天
 * @param {IsourceType} sType 要格式化的日期数据，为空时默认为当天
 * @param {IinitTypeBend | IinitTypeFend} iType 要格式化的日期数据，为空时默认为当天
 * @returns 模版内容字符串数组
 */
export default (cliConf: ICliConf, sType: IsourceType, iType: IinitTypeBend | IinitTypeFend) => {
	return 'test';
};
